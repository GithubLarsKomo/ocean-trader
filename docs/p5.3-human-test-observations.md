# P5.3 — Human-test observations

## 2026-08-31 · first Coolify / iPhone run

Observed on the human-acceptance candidate before H1:

1. **Rudder presentation mismatch** — HUD/control indication and the 3D rudder did not use the same visible sign convention.
2. **Immediate lateral motion on page open** — wind/current and/or a restored in-progress attempt could continue before the tester had deliberately issued a bridge command.

Human-gate impact:

- H-02: **FAIL / retest required**
- H-05: **FAIL / retest required**
- H-06: **blocked pending H1 retest**
- H-09/H-10: rudder visual consistency requires retest on iPhone

## P5.3-H1 correction

Branch: `feat/p5.3h1-human-test-fixes`

- one positive=STBD / negative=PORT rudder presentation convention;
- HUD label and rudder gauge derive from the same presentation helper;
- the current scene renderer's legacy mesh sign is isolated behind a tested renderer adapter;
- restored physical state and campaign damage remain intact;
- restored bridge commands are neutralized to STOP / MIDSHIPS / BOW OFF;
- fresh and restored manoeuvres start paused;
- the fixed-step physics loop begins only after a deliberate engine, rudder or bow-thruster input;
- RESET returns to the same safe paused bridge-ready state.

## Required retest

After H1 is deployed, verify on the same iPhone:

1. load Alongside and do not touch controls: ship position/heading must remain stationary;
2. move rudder to STBD 35°: HUD, gauge and 3D blade must all show STBD consistently;
3. repeat PORT 35° and MIDSHIPS;
4. issue the first bridge command and verify wind/current then begin affecting the vessel normally;
5. reload an interrupted campaign manoeuvre: damage/motion state must be retained, but active bridge controls must be neutral and the manoeuvre paused until deliberate resume.
