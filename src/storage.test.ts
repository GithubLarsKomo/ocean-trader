import{describe,expect,it}from'vitest';import{createInitialState}from'./domain';import{clearState,loadState,parseState,saveState,SAVE_KEY,V2_KEY,V3_KEY}from'./storage';
function memory(){const data=new Map<string,string>();return{getItem:(k:string)=>data.get(k)??null,setItem:(k:string,v:string)=>void data.set(k,v),removeItem:(k:string)=>void data.delete(k),data};}
describe('save schema v4',()=>{it('roundtrips financing state',()=>{const store=memory(),state=createInitialState();saveState(store,state);expect(parseState(store.data.get(SAVE_KEY)!)).toEqual(state);});it('migrates v3 commerce state with empty loans',()=>{const store=memory(),fresh=createInitialState();const{loans,...v4}=fresh;store.setItem(V3_KEY,JSON.stringify({...v4,schemaVersion:3}));const migrated=loadState(store)!;expect(migrated.schemaVersion).toBe(4);expect(migrated.loans).toEqual([]);expect(migrated.shipMarket.length).toBeGreaterThanOrEqual(4);});it('still migrates v2 fleet with technical ship data',()=>{const store=memory(),fresh=createInitialState();const oldVessels=fresh.vessels.map(({classId,builtYear,fuelCapacityTonnes,...v})=>v);store.setItem(V2_KEY,JSON.stringify({schemaVersion:2,campaignDate:fresh.campaignDate,cash:123,reputation:50,vessels:oldVessels,contracts:fresh.contracts,voyages:[],completedContracts:[],transactionLog:[]}));const migrated=loadState(store)!;expect(migrated.schemaVersion).toBe(4);expect(migrated.vessels[0].classId).toBe('handysize');expect(migrated.loans).toEqual([]);});it('clears all historical save keys',()=>{const store=memory();for(const k of['ocean-trader.save.v1',V2_KEY,V3_KEY,SAVE_KEY])store.setItem(k,'x');clearState(store);expect(store.data.size).toBe(0);});});
